# Core web framework
fastapi==0.115.0

# ASGI server (runs FastAPI)
uvicorn[standard]==0.30.0

# For calling Ollama via subprocess OR API
requests==2.32.3

# Optional: for CORS (so React frontend can call backend)
python-multipart==0.0.9

